/**
 * @file (auth)/layout.tsx
 * @description Layout para páginas de autenticación (sin header/footer)
 * @author Turns Team
 * @created 2025-11-07
 */

export default function AuthLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}

